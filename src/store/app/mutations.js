export function setCurrentCity(state, { currentCity }) {
  state.currentCity = currentCity;
}

export function setPrevision(state, { prevision }) {
  state.prevision = prevision;
}

export function setRows(state, { rows }) {
  state.rows = rows;
}
